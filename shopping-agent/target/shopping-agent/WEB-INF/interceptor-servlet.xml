<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="  
    http://www.springframework.org/schema/beans   
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd  
    http://www.springframework.org/schema/context  
    http://www.springframework.org/schema/context/spring-context-3.0.xsd 
    http://www.springframework.org/schema/mvc
	http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd 
	http://www.springframework.org/schema/tx
	http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
	http://www.springframework.org/schema/aop  
    http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
    ">
	<!-- 在web.xml中配置该xml生效; 拦截器,/**表示所有url，若多个拦截器则按照配置文件中先后顺序执行 -->

	<!-- 未登录拦截器 -->
	<mvc:interceptors>
		<!-- 注册 -->
		<mvc:interceptor>
			<mvc:mapping path="/register/**" />
			<bean class="com.renren.shopping.interceptor.CheckUnLoginInterceptor" />
		</mvc:interceptor>
		<!-- 登录 -->
		<mvc:interceptor>
			<mvc:mapping path="/login/**" />
			<bean class="com.renren.shopping.interceptor.CheckUnLoginInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>

	<!-- 登录拦截器 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/u/**" />
			<bean class="com.renren.shopping.interceptor.CheckLoginInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>

	<!-- 后台管理员拦截器 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/console/**" />
			<bean class="com.renren.shopping.interceptor.CheckAdminInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>


	<!-- MethodInterceptor,先执行HandlerInterceptor -->


	<!-- 手机 登录拦截器 -->
	<bean id="loginRequiredInterceptor"
		class="com.renren.shopping.interceptor.mobile.LoginRequiredInterceptor"></bean>
	<aop:config>
		<!--切入点 -->
		<aop:pointcut id="mobile_point"
			expression="execution(* com.renren.shopping.controllers..*.*.*(..))" />
		<!--在该切入点使用自定义拦截器 ,按照先后顺序执行 -->
		<aop:advisor pointcut-ref="mobile_point" advice-ref="loginRequiredInterceptor" />
	</aop:config>
</beans>